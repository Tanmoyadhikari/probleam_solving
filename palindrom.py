"""
1.Define a number.
2.splite the number and assign the parts on a array 
3.reverse the array.
4.Match the main array and reversed array.
5.If True the number is palindrom . If False the number isn't palindrom.

#Testing Instance ...
    defining a object -->    obj1 = IsPalindrom(your_number)
    defining a object -->    obj3 = IsPalindrom(your_number)

    calling the object-->    print(obj1.checknum())
    calling the object-->    print(obj3.checknum())
"""
class IsPalindrom():
    def __init__(self,number):
        self.num = number
    def checknum(self):
        return 
    """def checknum(self):
        numarr = str(self.num)
        newarr =list(numarr)
        # for x in numarr:
        #     newarr.append(x)
        rearr = newarr[::-1]
        if newarr == rearr:
            return "palindrome number"
        else:
            return "Not palindrome Number"
        """
        


#Check in dayclass-3.py file.....
